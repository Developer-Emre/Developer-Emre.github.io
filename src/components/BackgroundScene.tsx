/**
 * BackgroundScene
 * Dekoratif arka plan — tek element, ::before/::after ile 3 katman.
 * aria-hidden: ekran okuyuculardan gizlenir.
 */
const BackgroundScene = () => (
  <div className="background-scene" aria-hidden="true" />
);

export default BackgroundScene;
